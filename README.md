# ChatBot
A basic chatbot workflow with FastAPI for backend and Streamlit for frontend using Google Gemini as the model

## Backend Setup
1. cd backend && pip install -r requirements.txt
2. Create a .env file with your GOOGLE_API_KEY
3. uvicorn main:app --reload

## Frontend Setup
1. cd frontend && pip install streamlit requests
2. streamlit run app.py

